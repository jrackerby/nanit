# Test dependencies for the vendored aionanit_jr suite. CI installs from this
# file (LAW 15: declare beside the suite, never a list in the workflow).
# aiohttp <3.14: test_rest.py constructs aiohttp.ClientResponse directly and
# 3.14 changed that signature -- 45 test-only failures, the library itself
# runs on whatever Home Assistant ships.
aiohttp>=3.13,<3.14
protobuf>=6,<8
pytest>=9,<10
pytest-asyncio>=1.3,<2
aioresponses>=0.7,<0.8
